#!/usr/bin/python3


import sys

n = len(sys.argv)

if n < 2:
    sys.stderr.write("Usage: ./markdown2html.py README.md README.html")
    exit(1)
if(FileExistsError):
    sys.stderr.write("Missing <filename>")
    exit(1)
else:
    exit(0)